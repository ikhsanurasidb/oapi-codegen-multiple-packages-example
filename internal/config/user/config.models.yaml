# yaml-language-server: $schema=https://raw.githubusercontent.com/oapi-codegen/oapi-codegen/HEAD/configuration-schema.json
package: gen_user
output: ../../gen/user/user_models.go
generate:
  models: true
output-options:
  # to make sure that all types are generated
  skip-prune: true
  include-tags:
    - user
  exclude-schemas:
    - Pet
    - Category
    - Tag
    - Customer
    - Address
    - Order
    - ApiResponse
    - UserArray